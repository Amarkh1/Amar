from flask import Flask
from threading import Thread

app = Flask('')

@app.route('/')
def home():
    return "PAST_SIMPLE_WORKSHEET['exercise1']['questions'][next_q]}"
                    )
                    return EXERCISE1
                else:
                    scoreBot is alive!"

def run():
    app.run(host='0.0.0.0', port=8080)

def keep_ = context.user_alive():
    t = Thread(target=run)
    t.daemon = True
    t.start()
